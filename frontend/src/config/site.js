/* ------------------------------------------------------------------
   ไฟล์นี้คงไว้เพื่อความเข้ากันได้ย้อนหลังเท่านั้น
   ค่า ORG_NAME / ORG_TAG จริงตอนนี้ถูกจัดการผ่าน content.SITE
   (แก้ไขได้ที่แผงควบคุมเนื้อหา > ตั้งค่าเว็บไซต์) — ดู src/data/defaultContent.js
------------------------------------------------------------------ */
import { DEFAULT_CONTENT } from '../data/defaultContent'

export const ORG_NAME = DEFAULT_CONTENT.SITE.orgName
export const ORG_TAG = DEFAULT_CONTENT.SITE.orgTag
